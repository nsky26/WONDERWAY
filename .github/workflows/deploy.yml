name: Deploy WonderWay to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build Application
        run: |
          npx ng build --configuration production --base-href /WONDERWAY/
          touch ./docs/.nojekyll
          touch ./.nojekyll
          if [ -f "./docs/index.csr.html" ]; then
            cp ./docs/index.csr.html ./docs/index.html
            cp ./docs/index.csr.html ./docs/404.html
            cp ./docs/index.csr.html ./index.html
            cp ./docs/index.csr.html ./404.html
          elif [ -f "./docs/browser/index.csr.html" ]; then
            cp ./docs/browser/index.csr.html ./docs/index.html
            cp ./docs/browser/index.csr.html ./docs/404.html
            cp ./docs/browser/index.csr.html ./index.html
            cp ./docs/browser/index.csr.html ./404.html
          elif [ -f "./docs/browser/index.html" ]; then
            cp ./docs/browser/index.html ./docs/404.html
            cp ./docs/browser/index.html ./index.html
            cp ./docs/browser/index.html ./404.html
          fi
          if [ -d "./docs/browser" ]; then cp -r ./docs/browser/* ./docs/ || true; fi

      - name: Commit and Push Built Site to Main
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs/ index.html 404.html .nojekyll
          git diff --quiet && git diff --staged --quiet || git commit -m "Update root & docs static build for GitHub Pages [skip ci]"
          git push origin main || true
